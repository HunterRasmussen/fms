package fms.services;


import fms.results.SingleEventResult;

public class SingleEvent {


    /**
     * Uses EventDao to find the specific event Id and grabs that info to create and EventModel
     * object which is then passed to SingleEventResult
     *
     * @param eventId contains a unique id for a specific event
     * @return see SingleEventResult class
     */
    SingleEventResult singleEvent(String eventId){
        return null;
    }
}
