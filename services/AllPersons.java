package fms.services;


import fms.results.AllPersonsResult;

public class AllPersons {

    /**
     * Uses  AuthTokenDao to get userID.  Then looks at PersonDao to get all persons that are
     * ancestors to the current userID.  Then makes an array of them to make an AllPersonsResult
     * object
     */
    AllPersonsResult allPersons(){
        return null;
    }
}
